<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mj.community.dao.DiscussPostMapper">

    <sql id="selectFields" >
        id,user_id,title,content,type,status,create_time,comment_count,score
    </sql>

    <sql id="insertFields" >
        user_id,title,content,type,status,create_time,comment_count,score
    </sql>

    <select id="selectDiscussPosts" resultType="discussPost">
        select <include refid="selectFields"></include>
        from discuss_post
        where status !=2
        <if test="userId!=0">
            and user_id =#{userId}
        </if>
        <if test="orderModel==0">
            order by  type desc,create_time desc
        </if>
        <if test="orderModel==1">
            order by  type desc,score desc,create_time desc
        </if>
        limit #{offset},#{limit};
</select>

    <select id="selectDiscussPostRows" resultType="int">
        select count(id)
        from discuss_post
        where status !=2;
    </select>

    <insert id="insertDiscussPost" parameterType="cn.mj.community.pojo.DiscussPost" keyProperty="id">
        insert into discuss_post (<include refid="insertFields"></include>) values
        (#{userId},#{title},#{content},#{type},#{status},#{createTime},#{commentCount},#{score})
    </insert>

    <select id="selectDiscussPostById" resultType="cn.mj.community.pojo.DiscussPost">
        select <include refid="selectFields"></include>
        from discuss_post
        where id = #{id}
    </select>

    <update id="updateCommentCounts">
        update discuss_post set comment_count = #{commentCount} where id = #{id}
    </update>

    <update id="updateDiscussPostScore">
        update discuss_post set score = #{score} where id = #{id}
    </update>

    <update id="updateDiscussPostType">
        update discuss_post set type = #{type} where id = #{id}
    </update>

    <update id="updateDiscussPostStatus">
        update discuss_post set status = #{status} where id = #{id}
    </update>
</mapper>